class musicChecks {
  constructor(client) {
    this.client = client;
    this.Discord = require("discord.js");
    this.embed = new this.Discord.MessageEmbed();
    this.returnData = {};
  }

  checkUser() {

  }

  checkPerms(channel) {
    
  }
}